# Java-RMI-Simple-Calculator
 Java RMI–based Math Calculator that supports basic arithmetic operations. It allows a client to remotely perform addition, subtraction, multiplication, and division using a server through Java Remote Method Invocation (RMI).
